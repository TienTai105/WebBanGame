import { FC } from 'react'
import ProductListPage from '../components/sections/ProductListPage'

/**
 * Product list page
 * (Header, Navigation, and Footer are rendered by App.tsx globally)
 */
const ProductList: FC = () => {
  return <ProductListPage />
}

export default ProductList
