services:
  - type: web
    name: webbangame
    runtime: node
    region: oregon
    buildCommand: "cd server && npm install && npm run build"
    startCommand: "bash start.sh"
    envVars:
      - key: NODE_ENV
        value: production
